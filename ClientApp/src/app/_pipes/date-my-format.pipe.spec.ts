import { DateMyFormatPipe } from './date-my-format.pipe';

describe('DateMyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
